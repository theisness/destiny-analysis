FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 复制 package.json 和 package-lock.json
COPY backend/package*.json ./

# 安装依赖
RUN npm install --production

# 复制后端源代码
COPY backend/ .

# 暴露端口
EXPOSE 5000

# 启动命令
CMD ["npm", "start"]

